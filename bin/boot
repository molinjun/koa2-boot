#!/usr/bin/env node
const program = require('commander');

program
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('new', 'generate a koa2 project')
  .command('list', 'list koa2 scaffolds')
  .parse(process.argv);
